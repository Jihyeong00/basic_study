import random

password = [random.randint(0, 1), random.randint(0, 1)random.randint(0, 1)]
number = random.randint(0, 10)

for i in range(3):
    for j in range(len(password)):
        if password[j] == 0 :
            ss
